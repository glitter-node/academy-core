import { initTheme, initThemeHandler, setThemeHandler } from './setThemeHandler';

export const handlerMap = {
  initTheme: initThemeHandler,
  setTheme: setThemeHandler,
};

export { initTheme, initThemeHandler, setThemeHandler };
